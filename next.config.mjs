/** @type {import('next').NextConfig} */
const nextConfig = {
    distDir: 'dist',
    output: 'export',  // This tells Next.js to generate static HTML for each page
  };
  
  export default nextConfig;
